global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: 'codereferee-server'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['codereferee-server:8080']
